name: CoSMA-Editor tests

on:
  pull_request:
    branches: ["trunk"]

jobs:
  backend:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        python-version: [ "3.10", "3.11", "3.12"]
    services:
      postgres:
        image: postgres:15.4
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: cosmae_db_user_ci
          POSTGRES_PASSWORD: cosmae_db_password_ci
          POSTGRES_DB: cosmae_data_ci
      # Set health checks to wait until postgres has started
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry
      - name: Install redis
        run: |
          curl -fsSL https://packages.redis.io/gpg | gpg --dearmor -o /usr/share/keyrings/redis-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/redis-archive-keyring.gpg] https://packages.redis.io/deb $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/redis.list
          sudo apt-get update
          sudo apt-get -y install redis
      - name: Setup Python # Set Python version
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'poetry'
          cache-dependency-path: |
            pyproject.toml
            poetry.lock
      # workaround for 3.8
      - name: Install dependencies
        run: |
          poetry env use ${{ matrix.python-version }}
          poetry install --no-interaction
      - name: Test with pytest
        run: |
          if [[ ${{ matrix.python-version }} == '3.10' ]]; then
            COSMAE_REDIS_PORT=6310 poetry run pytest -c pytest_ci.ini --cov-report html:coverage-${{ matrix.python-version }}
          fi
          if [[ ${{ matrix.python-version }} == '3.11' ]]; then
            COSMAE_REDIS_PORT=6311 poetry run pytest -c pytest_ci.ini --cov-report html:coverage-${{ matrix.python-version }}
          fi
          if [[ ${{ matrix.python-version }} == '3.12' ]]; then
            COSMAE_REDIS_PORT=6312 poetry run pytest -c pytest_ci.ini --cov-report html:coverage-${{ matrix.python-version }}
          fi
        env:
          COSMAE_CI: "True"
          COSMAE_DB_NAME: cosmae_data_ci
          COSMAE_DB_USER: cosmae_db_user_ci
          COSMAE_DB_PASSWORD: cosmae_db_password_ci
          COSMAE_PG_CONF: localhost:5432:cosmae_data_ci:cosmae_db_user_ci:cosmae_db_password_ci
          COSMAE_PG_SERVICE_FILE: "[cosmae_service]\nhost=localhost\nuser=cosmae_db_user_ci\ndbname=cosmae_data_ci\nport=5432"
          COSMAE_DJANGO_KEY: ${{ secrets.COSMAE_DJANGO_KEY }}
          COSMAE_REDIS_PASSWORD: ""
          COSMAE_REDIS_CONF: ""
      - name: Upload pytest test results
        uses: actions/upload-artifact@v3
        with:
          name: pytest-results-${{ matrix.python-version }}
          path: coverage-${{ matrix.python-version }}
        # Use always() to always run this step to publish test results when there are test failures
        if: ${{ always() }}

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "ui"
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"
          cache-dependency-path: "ui/package-lock.json"

      - name: Install frontend dependencies
        run: npm ci
      - name: Lint frontend
        run: npm run lint
      - name: test frontend
        run: npm run test
