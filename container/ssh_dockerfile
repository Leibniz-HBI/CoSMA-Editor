FROM ubuntu:24.04
RUN apt-get update \
  && apt-get install -y openssh-server \
  && rm -rf /var/lib/apt/lists/*
RUN mkdir /run/sshd
RUN mkdir /credentials
RUN ln -f -s /credentials/passwd /etc/passwd
RUN ln -f -s /credentials/shadow /etc/shadow
RUN ln -f -s /credentials/group /etc/group
